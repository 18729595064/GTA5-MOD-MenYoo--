1、放置文件。
GTA5
└─ menyooStuff
   └─ Language
      ├─ Language.readme.txt
      ├─ French.json
      └─ Chinese.json   ← 放这里
2、F8 → Settings → Menu Fonts。然后几个全部改成：Normalish
3、F8 → Settings → Language → Reload Language Files（重新加载语言文件）
4、F8 → Settings → Language → Chinese
